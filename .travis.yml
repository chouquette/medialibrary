language: cpp
compiler:
  - gcc
  - clang
script: mkdir build && cd build && cmake .. && make && ./test/unittest

before_install:
    - sudo add-apt-repository -y ppa:videolan/master-daily
    - sudo apt-get update -qq
    - sudo apt-get install -y libvlc-dev libvlccore-dev

